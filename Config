# -*-perl-*-

package.DfObjectFactory = {
    interfaces = (1.0);
    deploy = {
        generic = true;
    };

    dependencies = {
        1.0 = {
            # For resolving concrete type for interface or abstract class
            Reflections = 0.9.x;

            # For creating proxy for abstract class
            # Comment out this since Reflections-0.9.x has dependency on it
            # To ease the pain of resolve major version conflicts
            # Javassist = 3.19.x;
        };
    };

    test-dependencies = {
        1.0 = {
            JUnit5 = 5.x;

            Mockito = 2.x;

            # Type builder
            Arsenal-reflect = 1.0;
        };
    };

    build-environment = {
        chroot = basic;
        network-access = blocked;
    };

    build-system = happytrails;

    build-tools = {
        1.0 = {
            OpenJDK8 = 1.1;
            HappierTrails = 3.4;

            # Team build logic
            DFJavaAntBuildLogic = 1.1;

            # Lombok support
            Lombok = 1.16.x;
            LombokUtils = 1.1;
        };
    };

    # For more information on how to declare targets for HappyTrails packages
    # see: https://w.amazon.com/?BrazilBuildSystem/HappyTrails/GettingStarted
    targets = {
        DfObjectFactory-1.0 = { type = javalibrary; };
    };

};
